/*
  This is overkill for a project of this design, but I like the extensibility.
  Interesting other redux action/reducer/database info combos to add:

  users (could store links persistently by user)
  submission data
*/

export * from './url'
